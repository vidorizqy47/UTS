<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uts.todolistapps">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.uts.todolistapps.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.uts.todolistapps.DataDiri" />
        <activity android:name="com.example.uts.todolistapps.MainActivity" />
        <activity android:name="com.example.uts.todolistapps.UpdateDataDiri" />
        <activity android:name="com.example.uts.todolistapps.MenuUtama" />
        <activity android:name="com.example.uts.todolistapps.Register" />
        <activity android:name="com.example.uts.todolistapps.DataUser" />
        <activity android:name="com.example.uts.todolistapps.UpdateUser" />
    </application>

</manifest>